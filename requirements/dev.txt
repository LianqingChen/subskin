# Development dependencies for SubSkin project
# Testing
pytest>=7.0.0          # Testing framework (lower minimum for older pip/Python)
pytest-cov>=3.0.0      # Coverage plugin (lower minimum for older Python)
pytest-mock>=3.5.0     # Mocking support (lower minimum)

# Code quality and formatting
# ruff requires Python 3.7+, comment out for Python 3.6
# ruff>=0.1.0          # Fast Python linter (replaces flake8, isort, etc.)
black>=22.0.0          # Code formatter (lower minimum)
mypy>=0.900            # Static type checking (lower minimum for older Python)
pre-commit>=2.0.0      # Git hooks for code quality (lower minimum)

# Documentation
mkdocs>=1.4.0          # Documentation site (lower minimum)
mkdocs-material>=8.0   # Material theme (lower minimum)
mkdocstrings[python]>=0.20.0 # Automatic API documentation (lower minimum)

# Development tools
ipython>=7.0.0         # Interactive Python shell (lower minimum)
jupyter>=1.0.0         # Notebook support for data exploration
pdbpp>=0.10.0          # Enhanced debugger

# Code analysis
bandit>=1.7.0          # Security linting
safety>=2.0.0          # Dependency vulnerability scanning
radon>=5.0.0           # Code metrics and complexity analysis

# Build and packaging
build>=0.10.0          # PEP 517 build backend
twine>=4.0.0           # Package uploading
setuptools>=50.0.0     # Package building (lower minimum)

# Database tools for development
alembic>=1.8.0        # Database migrations (lower minimum)
sqlalchemy-utils>=0.38.0 # Additional SQLAlchemy utilities (lower minimum)